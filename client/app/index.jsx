// This is only for debugging

import { Redirect } from "expo-router";

export default function Index() {
    // Change the href to any of the groups you want to work on.
  return <Redirect href="/(tabs)" />;
}